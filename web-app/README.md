⭐ Intalls in project
npm create vite@latest app --template react 
-> สร้างReact เลิอกใช้ TypeScript เพื่อให้ตรงกับโปรเจค  มีการตรวจตัวเเปรชนิดข้อมูล มากว่าJavascirpt 
**Reactอัพใหม่ตลอด หน้าเปลี่ยนไปตลอด อะไรใหม่

npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

npm install react-router-dom :ติดตั้ง React Router เพื่อเชื่อมหลายหน้า
⭐ อ่านเพิ่มเติม 
import girlImg from '../assets/girl.png' การเอาภาพเข้า